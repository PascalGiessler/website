---
import projects from "../../collections/projects.json";
import Layout from "../../layouts/main.astro";
---

<Layout
  title="Projekte"
  description="Aktuelle Projekte von Dr. Pascal Giessler: ContentNexus und Principal Stack."
  keywords="Pascal Giessler, Projekte, ContentNexus, Principal Stack, KI, Open Source"
>
  <main class="relative z-10 max-w-3xl mx-auto px-6 pt-28 pb-24 lg:pt-36">

    <div class="mb-14">
      <p class="section-label mb-4 fade-up delay-1">Projekte</p>
      <h1
        class="fade-up delay-2"
        style="font-family: 'Cormorant', Georgia, serif; font-size: clamp(2.4rem, 5vw, 3.8rem); font-weight: 300; line-height: 1.1; letter-spacing: -0.02em; color: var(--text);"
      >
        Was gerade <em style="font-style: italic; color: var(--gold-light);">entsteht</em>
      </h1>
    </div>

    <div class="fade-up delay-3 grid grid-cols-1 gap-4">
      {projects.map((project) => (
        project.url ? (
          <a
            href={project.url}
            target="_blank"
            class="writing-card"
            style="text-decoration: none;"
          >
            <p class="writing-card-label">{project.de.status}</p>
            <h3 class="writing-card-title">{project.name}</h3>
            <p class="writing-card-desc">{project.de.description}</p>
            <p class="mt-4" style="font-family: 'JetBrains Mono', monospace; font-size: 0.62rem; letter-spacing: 0.1em; color: var(--gold-dim);">
              Besuchen ↗
            </p>
          </a>
        ) : (
          <div
            class="writing-card"
            style="cursor: default;"
          >
            <p class="writing-card-label">{project.de.status}</p>
            <h3 class="writing-card-title">{project.name}</h3>
            <p class="writing-card-desc">{project.de.description}</p>
          </div>
        )
      ))}
    </div>

  </main>
</Layout>
